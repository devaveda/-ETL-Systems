# -ETL-Systems
Built a Spring Boot–based ETL system that ingests data from API and CSV sources, normalizes records, stores them in PostgreSQL, and exposes analytics endpoints. Implemented incremental ingestion, ETL scheduling, Docker deployment, and monitoring APIs for health and statistics.
